#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <list>
#include <stack>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    stack<int> s,s1,s2;
    int N,x,h,recieve,count;
    cin >> N;

    while (N--) {
        cin >> x;
        s.push(x);

    }

}
